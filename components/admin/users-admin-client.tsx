"use client";
import { useEffect, useState } from "react";
type UserRow = { id: string; name: string; email: string; role: "ADMIN" | "USER"; isActive: boolean; passwordResetRequired: boolean };
export default function UsersAdminClient() { const [users,setUsers]=useState<UserRow[]>([]); const [name,setName]=useState(""); const [email,setEmail]=useState(""); const [password,setPassword]=useState(""); const [role,setRole]=useState<"ADMIN"|"USER">("USER"); const [error,setError]=useState<string|null>(null); const [message,setMessage]=useState<string|null>(null); async function load(){const r=await fetch('/api/admin/users'); const b=await r.json(); if(!r.ok) throw new Error(b.error||'Load failed'); setUsers(b.users||[])} useEffect(()=>{load().catch(e=>setError(e.message))},[]); async function create(){setError(null); const r=await fetch('/api/admin/users',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({name,email,password,role})}); const b=await r.json().catch(()=>({})); if(!r.ok){setError(b.error||'Create failed');return;} setMessage('User created'); setName(''); setEmail(''); setPassword(''); await load();} async function update(user:UserRow, data:Record<string,unknown>){const r=await fetch(`/api/admin/users/${user.id}`,{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)}); const b=await r.json().catch(()=>({})); if(!r.ok){setError(b.error||'Update failed');return;} setMessage('User updated'); await load();} return <section className="space-y-6"><div className="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-700">Admin users</p><h1 className="mt-2 text-2xl font-bold text-slate-950">User Management</h1></div>{error&&<p className="rounded-xl bg-red-50 px-4 py-3 text-sm text-red-700">{error}</p>}{message&&<p className="rounded-xl bg-emerald-50 px-4 py-3 text-sm text-emerald-700">{message}</p>}<div className="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm grid gap-3 md:grid-cols-5"><input value={name} onChange={e=>setName(e.target.value)} placeholder="Name" className="rounded-xl border-slate-300 px-3 py-2.5 text-sm"/><input value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email" className="rounded-xl border-slate-300 px-3 py-2.5 text-sm"/><input value={password} onChange={e=>setPassword(e.target.value)} placeholder="Temp password" type="password" className="rounded-xl border-slate-300 px-3 py-2.5 text-sm"/><select value={role} onChange={e=>setRole(e.target.value as any)} className="rounded-xl border-slate-300 px-3 py-2.5 text-sm"><option>USER</option><option>ADMIN</option></select><button onClick={create} className="rounded-xl bg-cyan-400 px-4 py-2.5 text-sm font-bold text-slate-950">Create user</button></div><div className="rounded-3xl border border-slate-200 bg-white shadow-sm overflow-x-auto"><table className="min-w-full text-sm"><thead className="bg-slate-50 text-left text-xs uppercase text-slate-500"><tr><th className="px-6 py-3">Name</th><th className="px-6 py-3">Email</th><th className="px-6 py-3">Role</th><th className="px-6 py-3">Active</th><th className="px-6 py-3 text-right">Actions</th></tr></thead><tbody className="divide-y divide-slate-100">{users.map(u=><tr key={u.id}><td className="px-6 py-4 font-semibold">{u.name}</td><td className="px-6 py-4">{u.email}</td><td className="px-6 py-4">{u.role}</td><td className="px-6 py-4">{u.isActive?'Yes':'No'}</td><td className="px-6 py-4 text-right space-x-2"><button onClick={()=>update(u,{role:u.role==='ADMIN'?'USER':'ADMIN'})} className="rounded-lg border px-3 py-2 text-xs font-bold">Toggle role</button><button onClick={()=>update(u,{isActive:!u.isActive})} className="rounded-lg border px-3 py-2 text-xs font-bold">{u.isActive?'Disable':'Enable'}</button><button onClick={()=>{const p=prompt('New password, min 8 chars'); if(p) update(u,{password:p});}} className="rounded-lg border px-3 py-2 text-xs font-bold">Reset password</button></td></tr>)}</tbody></table></div></section> }
