"use client";
import { useState } from "react";
export default function AdminMessagingClient(){const [recipientEmail,setRecipientEmail]=useState(''); const [title,setTitle]=useState(''); const [messageHtml,setMessageHtml]=useState(''); const [result,setResult]=useState<string|null>(null); const [error,setError]=useState<string|null>(null); async function send(){setError(null);setResult(null);const r=await fetch('/api/admin/messages',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({recipientEmail:recipientEmail||null,title,messageHtml})});const b=await r.json().catch(()=>({}));if(!r.ok){setError(b.error||'Send failed');return;}setResult(`Sent to ${b.sentCount} user(s)`);setTitle('');setMessageHtml('');}return <section className="space-y-6"><div className="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-700">Admin messaging</p><h1 className="mt-2 text-2xl font-bold text-slate-950">Send Admin Message</h1><p className="mt-2 text-sm text-slate-500">Leave recipient blank to send to all active users.</p></div>{error&&<p className="rounded-xl bg-red-50 px-4 py-3 text-sm text-red-700">{error}</p>}{result&&<p className="rounded-xl bg-emerald-50 px-4 py-3 text-sm text-emerald-700">{result}</p>}<div className="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm grid gap-4"><input value={recipientEmail} onChange={e=>setRecipientEmail(e.target.value)} placeholder="Recipient email, optional" className="rounded-xl border-slate-300 px-3 py-2.5 text-sm"/><input value={title} onChange={e=>setTitle(e.target.value)} placeholder="Title" className="rounded-xl border-slate-300 px-3 py-2.5 text-sm"/><textarea value={messageHtml} onChange={e=>setMessageHtml(e.target.value)} placeholder="Message" className="min-h-36 rounded-xl border-slate-300 px-3 py-2.5 text-sm"/><button onClick={send} className="w-fit rounded-xl bg-cyan-400 px-5 py-2.5 text-sm font-bold text-slate-950">Send message</button></div></section>}
