"use client";
import { useState } from "react";
type AiRow={rowNumber:number;description:string;qty:string;unit:string;unitPrice:string;suggestions:Array<any>;selected?:boolean;selectedIndex?:number};
export default function AiHelperClient(){const [file,setFile]=useState<File|null>(null);const [source,setSource]=useState('both');const [rows,setRows]=useState<AiRow[]>([]);const [meta,setMeta]=useState<any>(null);const [error,setError]=useState<string|null>(null);const [message,setMessage]=useState<string|null>(null);const [busy,setBusy]=useState(false);async function process(){if(!file){setError('Select .xlsx first');return;}setBusy(true);setError(null);setMessage(null);const form=new FormData();form.set('file',file);form.set('source',source);try{const r=await fetch('/api/ai-helper/process-file',{method:'POST',body:form});const b=await r.json();if(!r.ok)throw new Error(b.error||'Process failed');setMeta({worksheetName:b.worksheetName,headerRowIndex:b.headerRowIndex,columns:b.columns});setRows((b.rows||[]).map((row:AiRow)=>({...row,selected:true,selectedIndex:0})));setMessage(`Processed ${(b.rows||[]).length} rows`);}catch(e){setError(e instanceof Error?e.message:'Process failed')}finally{setBusy(false)}}async function save(){const r=await fetch('/api/ai-helper/save',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({referenceNumber:`AI-${Date.now()}`,meta,rows})});const b=await r.json().catch(()=>({}));if(!r.ok){setError(b.error||'Save failed');return;}setMessage(`Saved AI project ${b.project.referenceNumber}`)}async function convert(){const selected=rows.filter(r=>r.selected).map(r=>({...r,selectedSuggestion:r.suggestions?.[r.selectedIndex||0]}));const r=await fetch('/api/ai-helper/convert-to-offer',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({referenceNumber:`FO-AI-${Date.now()}`,clientSnapshot:{},rows:selected})});const b=await r.json().catch(()=>({}));if(!r.ok){setError(b.error||'Convert failed');return;}window.location.href=`/offer/${b.project.id}`;}return <section className="space-y-6"><div className="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-cyan-700">AI Helper</p><h1 className="mt-2 text-2xl font-bold text-slate-950">Spreadsheet Matching</h1><p className="mt-2 text-sm text-slate-500">Upload spreadsheet, detect columns, remove footers, match catalog, then convert selected rows to an offer.</p></div>{error&&<p className="rounded-xl bg-red-50 px-4 py-3 text-sm text-red-700">{error}</p>}{message&&<p className="rounded-xl bg-emerald-50 px-4 py-3 text-sm text-emerald-700">{message}</p>}<div className="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm flex flex-wrap gap-3"><input type="file" accept=".xlsx" onChange={e=>setFile(e.target.files?.[0]||null)} className="rounded-xl border border-slate-300 px-3 py-2.5 text-sm"/><select value={source} onChange={e=>setSource(e.target.value)} className="rounded-xl border-slate-300 px-3 py-2.5 text-sm"><option value="both">Foreign + Local</option><option value="foreign">Foreign</option><option value="local">Local</option></select><button onClick={process} disabled={busy} className="rounded-xl bg-cyan-400 px-5 py-2.5 text-sm font-bold text-slate-950">Process file</button>{rows.length>0&&<><button onClick={save} className="rounded-xl border px-5 py-2.5 text-sm font-bold">Save AI project</button><button onClick={convert} className="rounded-xl bg-slate-900 px-5 py-2.5 text-sm font-bold text-white">Convert to offer</button></>}</div>{meta&&<pre className="rounded-2xl bg-slate-950 p-4 text-xs text-cyan-100">{JSON.stringify(meta,null,2)}</pre>}<div className="rounded-3xl border border-slate-200 bg-white shadow-sm overflow-x-auto"><table className="min-w-full text-sm"><thead className="bg-slate-50 text-left text-xs uppercase text-slate-500"><tr><th className="px-4 py-3">Use</th><th className="px-4 py-3">Row</th><th className="px-4 py-3">Original description</th><th className="px-4 py-3">Qty</th><th className="px-4 py-3">Match</th></tr></thead><tbody className="divide-y divide-slate-100">{rows.map((row,index)=><tr key={row.rowNumber}><td className="px-4 py-3"><input type="checkbox" checked={Boolean(row.selected)} onChange={e=>setRows(cur=>cur.map((r,i)=>i===index?{...r,selected:e.target.checked}:r))}/></td><td className="px-4 py-3">{row.rowNumber}</td><td className="px-4 py-3 max-w-xl">{row.description}</td><td className="px-4 py-3">{row.qty} {row.unit}</td><td className="px-4 py-3"><select value={row.selectedIndex||0} onChange={e=>setRows(cur=>cur.map((r,i)=>i===index?{...r,selectedIndex:Number(e.target.value)}:r))} className="rounded-xl border-slate-300 px-3 py-2 text-xs">{row.suggestions?.length?row.suggestions.map((s,i)=><option key={s.id} value={i}>{s.itemCode||s.productType} — {s.descriptionPlain?.slice(0,80)}</option>):<option>No match</option>}</select></td></tr>)}</tbody></table></div></section>}
